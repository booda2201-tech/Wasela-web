export const environment = {
  production: true,
  /** Same host as site: `'/api'`. API on another host: full URL e.g. `https://api.example.com/api`. */
  apiBaseUrl: '/api',
  /** Same host: `''` (relative `/media/...`). Else match your API host, e.g. `https://api.example.com`. */
  apiOrigin: '',
};
